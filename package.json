{
  "name": "site-intencional",
  "version": "1.0.0",
  "private": true,
  "description": "E-commerce com Busca Semântica, Intenções Espirituais e Catálogo Inteligente",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,magenta\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && npm --prefix backend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
